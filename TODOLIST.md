# DAPA NEWS 개선 작업 목록

기준일: 2026-07-15

## 목표

- 동일 사건을 다룬 반복 보도와 동일 기획·연재 기사 중 순위가 가장 높은 원문 기사 1건만 발송한다.
- `방위사업청` 또는 `방사청`이 제목·RSS 설명·출처에 명시된 공개 기사는 일반 관련성 필터에서 누락하지 않는다.
- 기관 기사 수집 보장은 중복 기사 무제한 노출을 의미하지 않는다. 동일 사건은 정해진 순위가 가장 높은 원문 1건만 남긴다.

## P0 - 중복 제거 정확도

- [x] 2026-07-15 회귀 픽스처 추가
  - `[불붙는 KAI 인수전] 알박기 한화 VS 수싸움 현대차...내년이 분수령`
  - `[불붙는 KAI 인수전] 비싸진 몸값에 독과점 논란까지...분할 매각안 솔솔`
  - 기대 결과: 동일 연재 머리말과 `KAI 인수` 사건을 공유하므로 중복군으로 묶고 조회수 또는 대체 순위가 높은 원문 1건 선정
- [x] 제목 정규화 단계에 언론사 접미사, 특수문자, 괄호형 연재 머리말을 분리하는 로직 추가
- [x] 동일한 비일반 괄호형 연재 머리말을 공유하면 동일 스토리 클러스터로 묶기
- [x] 제목 토큰뿐 아니라 RSS `description` 토큰도 중복 판정 입력으로 사용
- [ ] 핵심 개체와 사건 지문을 생성해 `기업/기관 + 대상 + 행위`가 같은 기사를 우선 통합
- [ ] 동일 URL, 정규화 제목, 연재 머리말, 사건 지문, 유사도 순으로 판정하는 단계형 중복 제거 구현
- [x] 대표 기사 선택 순서를 `실제 조회수 > RSS/Google News 노출순위 > 공식성 > 최신성`으로 고정
- [x] 실제 조회수가 제공되지 않은 기사에 조회수를 추정하거나 생성하지 않기
- [x] 기사 제목·내용을 축약·합성·재작성하지 않고 선택된 원문 제목과 URL 그대로 발송

완료 기준:

- 2026-07-10 대통령 표창·무인수상정 사례와 2026-07-15 KAI 인수전 사례에서 순위가 가장 높은 원문 1건만 선택된다.
- `K2 전차 수출 계약`과 `K2 전차 변속기 시험`처럼 대상만 같고 사건이 다른 기사는 유지된다.
- 서로 다른 방사청 발표가 동일 기관 키워드만으로 합쳐지지 않는다.

## P0 - 방사청 기사 수집 보장

- [x] `방위사업청`, `방사청`을 기관 필수 키워드로 별도 관리
- [x] 제목·RSS 설명·출처 중 하나에 기관 필수 키워드가 있으면 일반 관련성 필터 전에 후보군에 보존
- [ ] 정책브리핑 방위사업청 RSS를 필수 수집원으로 지정하고 수집 성공 여부와 기사 수를 로그에 기록
- [x] Google News에서 `"방위사업청" OR "방사청"` 전용 쿼리를 일반 섹션 쿼리와 독립 실행
- [ ] 기관 기사에는 `is_agency_article`, `agency_match_field`, `agency_terms` 메타데이터 부여
- [ ] 기관 기사 누락 감시 지표 추가: 접근 가능한 필수 수집원 기준 후보 수집 누락률 0%
- [ ] 필수 수집원이 실패하거나 기관 전용 쿼리까지 모두 실패하면 Actions를 경고 또는 실패 처리

완료 기준:

- 제목, 설명 또는 출처에 `방위사업청`/`방사청`이 있는 유효 기사는 후보군에서 제거되지 않는다.
- 기관 기사도 동일 사건 중복 제거를 적용하며 전체 기사와 같은 대표 선택 순위를 사용한다.
- 필수 RSS 장애를 빈 기사 정상 상태로 오인하지 않고 로그와 Actions 결과에서 구분한다.

## P1 - 데이터와 품질 측정

- [ ] `Article` 모델과 RSS 파서에 `description`, `canonical_url`, `story_cluster_id` 추가
- [ ] 최근 실제 발송본을 날짜별 익명 회귀 데이터셋으로 저장
- [ ] 중복 판정 결과에 대표 기사, 제외 기사, 판정 근거와 점수를 기록
- [ ] 일일 발송 전 중복률, 기관 기사 후보 수, 기관 기사 최종 선정 수를 출력
- [ ] 최소 30일 백테스트에서 중복 기사 포함률 2% 이하와 서로 다른 사건 오병합률 1% 이하 확인
- [ ] CI에서 회귀 테스트를 통과한 커밋만 예약 발송하도록 GitHub Actions 품질 게이트 추가

## P2 - 선택적 고도화

- [ ] 제목·RSS 설명만으로 애매한 쌍에 한해 임베딩 또는 LLM 유사도 판정 적용 검토
- [ ] 원문 본문은 저작권과 사이트 정책을 확인한 출처에 한해 선택적으로 추출하고 저장하지 않기
- [ ] 관리자가 일일 스토리 클러스터와 제외 사유를 확인할 수 있는 품질 리포트 추가

## 작업 원칙

- 단순 공통 키워드 하나만으로 중복 처리하지 않는다.
- 결정적 규칙을 먼저 적용하고 의미 기반 판정은 애매한 경우에만 사용한다.
- 중복 제거 임계값 변경 전 실제 발송본 회귀 테스트를 먼저 추가한다.
- 기관 우선수집 규칙은 일반 기사 품질 필터를 약화시키지 않도록 별도 경로로 구현한다.
